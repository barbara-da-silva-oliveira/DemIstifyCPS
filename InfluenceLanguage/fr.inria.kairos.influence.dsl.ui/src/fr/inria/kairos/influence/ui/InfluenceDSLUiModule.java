/*
 * generated by Xtext 2.34.0
 */
package fr.inria.kairos.influence.ui;

import org.eclipse.ui.plugin.AbstractUIPlugin;
import org.eclipse.xtext.resource.IResourceDescription;

import com.google.inject.Binder;
import com.google.inject.multibindings.Multibinder;

import fr.inria.kairos.influence.MyIndexChangeListener;

/**
 * Use this class to register components to be used within the Eclipse IDE.
 */
public class InfluenceDSLUiModule extends AbstractInfluenceDSLUiModule {

	public InfluenceDSLUiModule(AbstractUIPlugin plugin) {
		super(plugin);
	}
	
	@Override
	public void configure(Binder binder) {
	    super.configure(binder);

	    System.out.println(">>> MyDslUiModule.configure() called");
	    
	    binder.bind(ListenerRegistrar.class).asEagerSingleton(); // This will be constructed and injected at startup
	    
//	    Multibinder<IResourceDescription.Event.Listener> listeners =
//	        Multibinder.newSetBinder(binder, IResourceDescription.Event.Listener.class);
//	    listeners.addBinding().to(MyIndexChangeListener.class);
	}
}
